/**
 * Plum wordmark, recreated in type — not the official brand asset.
 * If you have the real SVG, drop it in `public/` and swap the markup here.
 */
export default function Logo() {
  return (
    <span className="logo" aria-label="Plum">
      plum
    </span>
  );
}
